/*Create a program to take full name from user and generate a username start with @,
followed by their full name and ends with underscore followed by the length of full name.*/

// function generateUsername(str) {
//   return `@${str}-${str.length}`;
// }

// console.log(generateUsername("JabirKhan"));
